import React from 'react';
import MovieCard from '../MovieCard';

const NewReleaseList = (props) => {
    return (
        <div>
            <p>New Release List</p>
        </div>
    );
}

export default NewReleaseList;